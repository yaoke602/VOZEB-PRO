---
title: 生产上线基线
description: VOZEB PRO 商业部署前的健康检查、调度、备份、监控、支付与合规清单
---

# 生产上线基线

应用代码可以提供稳定能力，但商业上线还依赖部署环境、支付商账号、监控平台和运营主体。宝塔、Docker、云容器和托管 PostgreSQL 使用同一检查标准。

## 当前验收证据边界

- 千川分析新增边界：本轮仅验证离线协议夹具、隔离 PostgreSQL 和本地页面；四分类与授权/同步代码不等于真实平台已验收。生产启用前需核对授权回调、应用权限、令牌轮换、并发同步、真实非零金额单位、标准推广/全域计划口径、私有素材链接有效期及用户级数据隔离。当前为手动、单分类事务同步，尚未完成大账户长周期容量测试、后台定时采集或快照保留策略；上线前制定完整数据库备份与数据保留方案。AI 查数和 MCP 已实现待实测：需用真实默认文本模型核对问题理解、账单与退款、长请求代理超时，确认 MCP 绑定用户与千川授权用户一致；AI 解读仅供参考，不提供自动投放操作。查数请求不是后台 Worker 任务，进程中断须由用户明确决定是否重新提交并计费，不自动重发。
- 当前本地发布门禁已通过：Web `470` 个测试文件通过、`4` 个按环境跳过，`2087` 项测试通过、`9` 项跳过；依赖审计为 0 个已知漏洞，ESLint、Prettier、TypeScript、63 页生产构建和 Standalone 资源检查均通过。Docs 的依赖审计、类型、索引和 36 页生产构建已通过。Playwright 在同一生产构建上完成桌面、390px、430px 共 `98` 项，另有 `14` 项按数据库环境或项目互斥范围跳过，覆盖安装、首页、Canvas、文本/图片/视频/音频、统一 Agent、删除持久化、套餐、个人中心双分页、支付订单 SSE 和响应式布局。本轮另外使用现有管理员登录态手动复核用户运营、基础设置、模型渠道、外部存储、数据备份和支付渠道的保存往返及 390px/430px 浅深主题；这些人工检查不计入 Playwright 数量。当前环境未配置隔离 `VOZEB_PRO_E2E_DATABASE_URL`，真实 PostgreSQL 支付 E2E 与多实例并发仍需在专用数据库执行。
- 生产页面 CSP 使用每请求随机 nonce，主题预加载脚本、Next.js 运行脚本与响应头使用同一 nonce；连续请求 nonce 不同，生产 `script-src` 不含 `unsafe-inline`，完整浏览器矩阵未出现 CSP 控制台错误。支付配置、对象存储配置、渠道密钥查看和业务备份导出/恢复的成功/失败路径已写入管理员审计日志，且不记录密码、动态码、密钥、证书或备份正文。管理员 TOTP MFA、登录环境变化提醒、本人分页登录记录、13 项职责和 8 个预设已完成代码；后台业务逐次密码/MFA 复核已取消。真实管理员设备启用、生产登录、设备丢失恢复、多职责越权拒绝、异常登录处置和审计演练仍是独立的上线治理阻断项。
- 开源发布安全基线已增加系统渠道代理允许列表与逻辑模型授权、公开 Session 白名单、IP/账号/设备三维认证限流、密码重置统一响应、支付表单结构化解析与隔离提交、GitHub Skill commit/内容哈希固定及默认文本模型安全整理，以及正式 Tag 镜像门禁、SPDX SBOM、Cosign digest 签名和 attestation。Agent Run 现在保存当次实际 Skill 的完整不可变执行快照，公开 Run 与 SSE 白名单不会返回这些内部规则。CodeQL 报告的 10 条告警已通过受控请求传输、结构化 URL/HTML 解析、无偏加密随机数和单次实体解码完成代码修复，未使用规则忽略；仍需由 `v0.0.6` 后续推送后的远端扫描确认告警关闭，本次本地提交不会上传 GitHub。许可证只作为来源元数据，不是导入门禁。代码与契约测试通过不代表真实反向代理、支付商、文本模型质量、GitHub OIDC 或多实例 PostgreSQL 已完成生产验收。
- 当前 PostgreSQL 审计实例的昱梦渠道已配置图片和视频逻辑模型，Base URL 已修正为 `https://zcbservice.aizfw.cn/kyyReactApiServer`，创建与查询使用完整 `/kyyReactApiServer/v2/model-center/tasks` 与 `/kyyReactApiServer/v2/model-center/tasks/:task_id` 路径，API Key 密文和 29 个预置模型未改动；内部代理会对旧 `token.myairealm.com` 门户地址做确定性修正，并避免重复拼接 `kyyReactApiServer`。本轮未创建收费任务，因此尚无修复后真实生成、模型质量、额度、计费、取消或 SLA 证据。New API 渠道继续提供文本、图片和音频默认模型。本地自建 TCP 接口已由完整协议注册表和 GlobalAiOpc 预置表逐项驱动，覆盖文生/参考图、异步查询、模型白名单、计费头、协议鉴权和实际媒体读取。夹具与代理测试只证明应用侧协议实现，不能替代真实供应商生成、额度、计费、取消、媒体质量或 SLA 证据。
- PostgreSQL 实际 59 张 `vozeb_pro_*` 表均能对应 Schema、Repository、外键、业务读取或旧表清理职责，不能按当前行数为零直接删除。两条历史图片任务因上游提交结果不确定、没有任务 ID 与查询路径进入 `needs_review`；这是避免重复生成和扣费的安全暂停，仍需管理员结合供应商账单与日志人工确认。
- PostgreSQL 的 Canvas、素材库、用户媒体和生成日志在线路径已收敛为用户范围分页或按后台配置的稳定批次；完整认证、提示词和生成记录快照只接受管理员备份事务。管理员永久删除用户时会在用户行锁事务内先分页快照媒体登记，再让数据库级联业务记录，提交后重新统计引用并清理本地/S3 对象。该自动化只证明代码编排和引用保护，尚未覆盖生产对象删除失败、跨业务共享引用、商业记录 `RESTRICT/SET NULL` 留存和正式注销政策；上线前必须用隔离 PostgreSQL、本地媒体与真实 S3 完成可恢复演练并保存证据。
- 邀请链接会在公开站点配置为回环地址时采用当前访问 IP 或域名，反向代理转发域名和 HTTPS 只在 `VOZEB_PRO_TRUSTED_PROXY_HOPS` 大于零时接受。生产验收必须确认该值与真实代理层数一致，避免生成内部地址或信任用户伪造的转发头。
- 七套 Compose 与 Render Blueprint 已通过结构化契约测试。五套主应用 Compose 锁定 app/Worker 同镜像、独立维护/Worker 令牌、Worker 命令、数据库最小权限、存活探针、持久卷、内置/外部 PostgreSQL 分界和宝塔 host 网络隔离；Worker 不再取得外部维护令牌。两套文档 Compose 锁定独立端口和发布/本地构建来源。Render 进一步锁定付费 Web/Worker、共享 Worker 令牌、Web 独立维护令牌、私网调用、`basic-256mb` PostgreSQL、10GB 持久盘和生产镜像脚本。v0.0.3 发布镜像已在一台 Linux x64 服务器暴露 Sharp 的 `libvips-cpp.so.8.18.3` 缺失，导致生成运维接口与 Worker 返回 500；生产 Dockerfile 现已显式复制当前架构原生包并在镜像构建阶段加载 Sharp。本机没有 Docker CLI，也没有 Render 账号部署证据，新镜像仍需完成 `docker compose config`、拉取与启动、两个生成运维接口、Worker 心跳、图片 WebP 预览、Blueprint 创建、宝塔安装和干净 Linux 恢复验证。
- 浏览器前序回归已覆盖主要桌面、390px/430px 和浅深主题页面；新构建切换后又使用现有管理员登录态复核后台、Agent、图片、视频、Canvas、短剧、素材和计费页面，桌面与 390px 加载为 210–396ms，根节点无横向溢出且控制台无 warning/error。生产发布仍需按目标浏览器、真实账号、真实媒体和生产网络完成最终人工验收。
- Canvas 独立重写已通过依赖清单、Canvas 源码标识和旧入口边界测试，当前画布不引入目标仓库或第三方画布包；这只是代码来源审计证据，不替代运营主体对版权、专利、商标、用户素材和第三方依赖许可证的法律审核。正式商业发布前仍需保留实现来源记录，并由法律顾问复核 clean-room 过程与全部供应商许可证。

## 健康检查

- 快客云独立脚本添加 `resolution: "720p"` 后已获得真实排队任务 ID；项目模板和运行时已同步发送清晰度，仍需构建后验证最终成片、下载和计费。排队成功不代表完整生成验收通过。

- 快客云视频协议已按用户提供的创建与查询契约接入，仅使用嵌套 `data[0].taskId`。查询故障继续核对原任务；创建结果不明保留计费流水并进入人工确认，不自动重复提交。未公开的首尾帧、取消、模型目录不启用。真实平台额度、任务失败语义、退款、最终媒体下载与网络可达性仍须独立验收，本地协议夹具不代表供应商验收通过。

- `GET /api/health/live`：只检查 Web 进程是否能响应，Docker Compose 使用该地址，避免数据库短暂异常导致应用容器反复重启。
- `GET /api/health/ready`：检查数据库连接、表结构、加密密钥和首个管理员；未完成安装或依赖异常时返回 HTTP `503`，用于反向代理、负载均衡和外部监控。
- `/api/install/status`：面向安装页面的安全状态，不应作为生产告警依据。

## 计划任务

配置至少 32 个字符的 `VOZEB_PRO_MAINTENANCE_TOKEN`，每 5 分钟调用：

```bash
curl -fsS -X POST \
  -H "Authorization: Bearer $VOZEB_PRO_MAINTENANCE_TOKEN" \
  https://vozeb-pro.example.com/api/maintenance/billing-orders/expire
```

宝塔使用计划任务，Docker 使用宿主机 Cron，云平台使用 Scheduler。任务可以重复执行，多实例会通过数据库锁避免重复关闭订单。

## 备份与恢复

- 后台“数据备份”导出与恢复要求当前管理员 Session 具备 `system.manage`，不再逐次要求当前密码或 TOTP；导出通过无请求体的独立 POST 返回 `private, no-store` 脱敏业务数据，恢复 multipart 只提交备份文件。该数据只用于 `account-config` 合并恢复账号、非敏感设置、公共提示词和生成记录；导入不会删除备份中缺失的用户或记录，PostgreSQL 使用事务与锁，文件 Provider 失败时回滚原快照。业务 JSON 不包含 Canvas、素材、媒体原件、短剧、订单和对象存储完整清单，不能作为整库灾难恢复包。
- 管理员 MFA Secret 不进入常规业务备份；恢复业务包不能替换、启用或关闭服务器现有 MFA。管理员丢失身份验证器时可在服务器执行 `pnpm -C web reset:admin-mfa -- --username <账号>`，命令会清除目标管理员 MFA 并撤销其全部 Session；文件 Provider 修改前保存恢复副本，PostgreSQL 使用事务。该命令是账号访问恢复能力，不替代完整灾难恢复演练。
- PostgreSQL 使用数据库平台或 `pg_dump` 做整库备份；支付流水、支付配置和密钥不依赖业务 JSON 备份。
- 本地媒体目录、S3/OSS 对象和对象存储配置分别备份；不要只备份数据库后假设媒体能够恢复。
- 至少保留每日自动备份、异地副本和定期恢复演练。备份任务成功不等于恢复可用。

## 监控与告警

根据部署环境选择 Sentry、OpenTelemetry、云厂商 APM、Prometheus/Grafana 或日志平台，不要求所有部署同时安装同一套组件。上线前至少覆盖：

- `/api/health/ready` 可用率和连续失败。
- Web CPU、内存、磁盘、容器重启和媒体容量。
- PostgreSQL 当前连接数、连接池等待、慢查询、锁等待和存储增长。
- API P50/P95/P99、HTTP 5xx、登录失败率和限流触发量。
- 图片、视频、音频和 Agent 的成功率、耗时、退款率和渠道冷却次数。
- 支付下单成功率、回调处理失败、长时间待支付订单和退款失败。

服务器日志可能包含内部错误细节，应限制管理员访问并设置保留周期；密钥、支付凭据、登录令牌和媒体签名参数不得写入日志平台。

私有媒体已按用户归属或绑定资源/用途/过期时间的短期签名读取，公开作品媒体每次重新校验当前公开版本；本地与代理 Range、HEAD、条件缓存、分层限流和进程内并发回收已有自动化证据。真实代理跳数、多实例限流、私有 S3、热点资源、文件句柄与外网下行仍必须在生产等价环境压测，代码上限不能替代容量和账单证据。

## 支付验收

每个真实启用的支付商都必须在沙箱或生产前测试：下单、成功支付、失败支付、重复/乱序/延迟回调、订单超时、全额退款、退款失败恢复和账单对账。支付宝若选择官方支付，需要验证电脑网站收银台跳转与同步/异步返回；若选择当面付，需要单独验证 `precreate` 二维码、扫码支付、二维码过期和异步通知，不能用官方支付测试代替当面付证据。当前自动退款按整笔订单处理；如果实际业务需要部分退款，必须先补齐订单、支付流水、积分回收和会计口径，再进行支付商部分退款验收。代码支持某个渠道不代表该商户账号、证书、回调域名和退款权限已经通过验证。

代码已锁定订单支付渠道，并在回调幂等处理前校验标准化渠道、订单金额和币种；已支付订单收到不同交易会进入冲突核对，而不是静默视为重复。正式验收仍要保存每个支付商的正常、重复、乱序、迟到、金额篡改、币种不符和跨渠道回调证据，并明确人工核对与补账责任人。

本地 TCP 协议夹具已经实际验证 Stripe、支付宝当面付、微信 Native 与 PayPly 的下单和退款请求、鉴权头及响应解析；兼容 HMAC 回调还覆盖了成功处理、重复事件、并发认领、无效签名和失败释放。本轮支付矩阵 8 个测试文件、38 项通过；它只能证明应用侧协议适配和回调编排可以收发预期数据，不能替代真实商户沙箱的签名证书、扫码/跳转、异步回调、退款权限和账单对账。

限时促销和单张优惠券已保存服务端价格快照，并接入锁券、释放、核销和整单退款关联。正式启用前仍必须使用生产等价 PostgreSQL 验证并发领券、同券并发下单、自动超时、迟到支付冲突和重复回调，并逐个支付商核对实收、退款及对账金额都等于订单 `amountCents`；自动化规则测试不能替代这些证据。

单层邀请奖励已接入注册、首笔可信实付、冷静期结算和整单退款事务，并提供网络/支付身份哈希风险信号与后台人工处置。正式启用前仍必须用真实 PostgreSQL、多个账号和实际支付回调验证原子绑定、重复事件、并发结算、月度/活动上限、维护调度、积分反向流水及优惠券撤销冲突；同时由运营确定风控阈值、冻结通知、申诉与误伤恢复流程。单一 IP 信号不能作为自动认定欺诈的唯一依据。

经营看板已提供累计订单成交率、活动订单成交率、用券订单成交率和邀请注册率。财务指标复用一条 PostgreSQL 摘要聚合，邀请指标按需读取独立摘要，不进入全局 Session；无分母时显示未知而不是伪造 `0%`。当前面板只是代码侧基础观测，仍需生产规模数据验证查询耗时，并由运营/财务确认正式时间窗、收入、留存、ARPU、复购、退款、税费、成本和毛利口径。

## 容量与压测

先以生产相同数据库、代理和存储进行 100、500、1000 并发分级测试，记录登录、列表、生成任务创建、SSE 连接、支付和后台查询的 P95/P99。根据瓶颈再调整 `VOZEB_PRO_DATABASE_POOL_MAX`、实例数、队列或缓存，不要仅靠增大连接池解决并发问题。

后台财务摘要已使用单次 PostgreSQL 聚合和订单/支付流水时间索引，同一查询同时返回订单、支付、渠道、对账以及活动/优惠券成交摘要；正常退款不再进入支付状态异常计数，订单与支付日期窗口也同时约束对账子查询。本地 PostgreSQL 16.14 的真实执行计划命中两张表的时间索引并在 0.701ms 完成，但两表当前没有订单数据；仍需在生产等价订单量下保存 P50/P95/P99、连接池/锁等待和真实支付商对账证据。

短剧成本概览已改为按用户、项目、任务类型和状态在 PostgreSQL 聚合，并增加项目定向索引；本地执行计划命中 `generation_tasks_user_project_idx`，Canvas、素材和会话分页也具备定向索引路径。该证据只覆盖当前小数据集，仍需在生产等价任务规模下记录 P50/P95/P99、连接池/锁等待和成本账本核对结果。

## 异步生成与计费闭环

图片、视频、音频和 Agent 任务需要在真实渠道下验证排队、超时、取消、重试、断线恢复、重复提交和卡死任务接管。每个任务都应能与原消费流水、失败退款、用户余额、生成记录和媒体登记逐项核对；重启和多实例竞争不能造成重复扣费、重复资产或“任务失败但积分未退”。

当前代码已提供页面定向唤醒、独立生成 Worker、PostgreSQL 租约、心跳就绪检查、HMAC 回调幂等登记和 `needs_review` 管理员接管。普通 Agent 生成完成后可由持久 Worker继续内部复盘，进程重启后仍可恢复，复盘最终不可用不会撤销成功媒体；`needs_revision` 只保存内部建议，不会自动重新创建上游媒体任务。用户端 SSE 断线时不按固定失败次数终止，而是继续等待 EventSource 自动重连并复用单个服务端 Run 状态核对；切换对话也只停止当前页面观察，不改变后台 Run。Planner 只按 Surface 与能力筛选已验证模型和用户显式 Skill，完整发送会话上下文、参考资产与项目快照，不再由平台叠加固定字符、输出 token 或节点数量上限；真实供应商上下文窗口和拒绝行为仍需单独验收。PostgreSQL Run 事件使用事务内 `NOTIFY` 和独立 `LISTEN` 连接即时唤醒 SSE，现有轮询只承担通知丢失兜底。本地协议矩阵已按注册能力核对任务完成、媒体解析/落盘、资产登记、查询路径、模型目录和鉴权头，并覆盖 Worker 查询、人工确认失败与退款。这些结果不能替代默认 Docker、宝塔、低内存、外部数据库、多实例和真实供应商下的无人页面运行、通知丢失、接管竞争、媒体登记与账本证据。

混合模型渠道可以按模型保存 OpenAI、Gemini、Seedance 2.0 或兼容协议与创建/查询路径，但代码适配不能替代供应商文档和真实密钥验收。上线前应逐渠道保存实际模型目录响应、分页证据及文本/图片/视频最小调用结果；上游未公开目录或使用不透明模型名时，必须由管理员维护模型能力与路由，不能把名称猜测当成生产证据。

PostgreSQL 在线扣费已使用定向的用户、套餐、每日钱包、额度和流水查询，完整认证快照只保留给备份或兼容路径。系统渠道的本地计费身份由服务端绑定用户、业务请求、逻辑模型、渠道、上游模型和调用类型生成，普通用户提交的计费幂等头不会直接成为流水键；流水请求指纹会拒绝同身份不同参数的重放，零积分调用也保留可撤销记录。退款补偿每次维护调度只执行一次真实尝试，后续节奏由外部 Scheduler 决定，不再内置“8 次后人工”或指数退避；Agent 文本字数要求只转发用户本轮约束，不再按固定三次重写并截断成功内容，邀请码与 CDK 也改用数据库唯一身份生成。该实现仍必须通过真实生成并发、锁等待、超时退款和余额对账验收；重试、人工补账、上游暂停和降级规则必须来自供应商公开约束、管理员配置或已有且可验证的资源保护契约，不能新增拍脑袋的固定次数、延时或输出上限。

## 安全事件与数据泄露响应

商业上线前需要单独准备并演练安全事件响应手册，不能把它拆散在“密钥加密”和“日志监控”两项里：

- 针对模型密钥、支付证书、维护令牌、OSS 凭据、Session 和个人数据泄露，明确发现、分级、隔离、吊销、轮换、取证、修复、用户通知和复盘责任人。
- 覆盖异常登录、越权访问、私有媒体暴露、数据库泄露和支付数据异常，保留必要证据，并按运营地区和合同要求判断是否需要通知用户、支付商、供应商或监管机构。
- 演练暂停受影响模型/支付渠道、撤销会话、轮换密钥、恢复备份和重新验证媒体及订单引用；不能因为处置事件而静默损坏生成记录、积分或财务数据。
- 依赖扫描、加密存储和已有健康探针只能作为技术基础，不能替代可执行的事件响应、通知和恢复证据。

## 法律与运营

站内 `/privacy` 和 `/terms` 只是产品级基础页面。正式运营前，必须由法律顾问按运营主体、服务地区、数据存储位置、第三方模型服务、退款规则、知识产权、内容审核和用户注销流程进行审核并替换为正式版本。面向中国大陆、欧盟或其他地区时分别核对当地适用法规。

登录用户可在个人中心导出自己的账户、积分、账务摘要、创作记录和注销申请 JSON；导出不会包含媒体二进制、密码、会话、API 凭据、支付原始载荷、临时签名 URL 或平台内部规划详情。该能力只覆盖基础数据访问权，不能代替正式的数据请求处理制度。

个人中心支持通过当前密码复核提交和撤回注销申请；后台“注销申请”一级页面支持管理员受理或拒绝，处理备注必填并写入审计记录。受理只代表进入线下身份复核和数据保留核对，不会自动停用账号或删除订单、创作数据与媒体。该闭环属于注销申请管理，不等于已经完成正式注销执行。

公开注册已要求用户显式勾选服务条款和隐私政策；服务端在用户创建事务中保存当时的两份协议版本、链接与同意时间，客户端不能自行指定版本，管理员代建账号也不会伪造同意。上线前仍需由法律负责人审核实际文本与版本发布流程，并明确协议更新后的重新同意规则、注销正式执行、创作数据删除与订单/支付法定保留边界，以及身份复核、响应时限和处置制度。第三方模型、支付、SMTP 和对象存储服务的数据处理与跨境传输情况应在正式政策中披露。

## 内容、客服与财务

- 作品发布已具备草稿、版本审核、稳定分享、公开广场、公开创作者主页、精选、举报、下架申诉、审计记录和动态媒体授权；用户或管理员先下架再永久删除时只清空发布快照、治理和互动记录，源素材、项目与媒体文件保持不变。当前用户公开界面只提供点赞/取消与作者关注，作品收藏、评论和评论举报的 UI、API、服务、计数、Schema 与后台治理均已移除；本人关注、粉丝和点赞历史按登录用户分页读取，并支持取关、取消点赞和拉黑，拉黑会解除双方关注并阻止再次关注。公开主页只展示资料身份公开作品与聚合统计，不公开邮箱、内部用户 ID 或关系名单。互动通知继续保留，7/30 天榜单只按有效点赞、精选和新鲜度执行基础反刷新账号过滤。默认人工审核，也可接入关键词或中立 HTTP 风险信号，但自动结果不会直接公开。这些代码能力不等于平台内容合规或反刷运营已经闭环，仍需用真实多账号验证关系隐私、拉黑/取消拉黑、下架/封禁联动，并完成侵权投诉、刷量规则、人工值班、处置时限、误伤复核、隐私规则、生产监控和真实演练证据。
- 短剧工作台的“内容审核”是用户确认剧本事实的生产 Gate，不是平台内容合规审核。正式运营仍需由平台举报/申诉流程、人工复审制度和审计证据承接。
- 需要明确年龄限制、禁止用途、AI 生成内容标识、用户上传内容授权、侵权投诉和下架流程；用户不能默认对输入素材或生成结果拥有不受限制的商业权利。
- 帮助中心和公告不能代替客服流程。至少明确客服入口、服务时间、支付问题、内容举报、隐私请求和投诉升级方式。
- 当前支持 CSV 支付账单导入与差异对账；正式运营仍需明确账单获取周期、异常处理责任、退款审批、发票/税务和财务留档规则。

## 供应商与安全治理

- 为模型渠道、支付商、SMTP、对象存储和通知服务建立供应商清单，记录价格、配额、数据使用、数据区域、保留期限、SLA、停服通知和替代渠道。代码支持某个 Provider 不等于供应商合同、商户权限和退款权限已经完成。
- 在用户协议中说明用户内容授权、生成内容权属不确定性、第三方模型训练/留存边界、品牌和人物素材授权责任，以及上游模型变更导致的结果差异。
- 落实 AGPL-3.0、第三方依赖、字体和素材许可证义务，保留许可证与版权声明；正式发布前完成依赖、容器镜像和密钥泄露扫描，并建立漏洞披露、修复和安全公告流程。
- 明确模型渠道故障、价格调整、配额耗尽、模型下线和内容安全服务不可用时的降级、暂停生成、用户通知和退款规则。

## 账户风控与商业经营缺口

下面事项不能由应用代码或宝塔面板自动完成，正式商业上线前必须由运营、财务、法律和技术负责人共同确认：

- **经营主体与资质**：确认运营主体、服务地区、域名与网络相关资质、支付商户资质、发票/税务主体及第三方模型、SMTP、支付和对象存储的合同与数据处理边界。AGPL-3.0 的网络服务开源义务仍需按实际二次开发和交付方式履行。
- **计价与成本**：固定积分与套餐的兑换口径，区分一次性积分、套餐每日积分和过期规则；建立模型调用、失败退款、存储、带宽、支付手续费和税费的成本表，设置上游余额、异常成本和毛利预警。当前经营看板不能直接当作利润报表。
- **权限与审计**：后台已在管理员角色内提供 13 项显式职责和 8 个预设，覆盖财务、客服与用户运营、内容审核、商品营销、上游运维、系统运维和只读审计；管理员不能授予超出自身范围的职责，也不能修改或删除职责范围高于自己的管理员。正式团队运营前仍应为每个账号分配最小职责，确定退款/积分调整的人工审批与定期权限复核制度。
- **事故与服务**：明确版本发布、回滚、值班、故障分级、状态通知、客服入口、响应时限和升级路径。健康探针、日志和告警是技术基础，不等于已经建立 SLA 或人工响应流程。
- **规模与增长**：定义注册、首个成功作品、生成成功率、付费转化、留存、ARPU、复购、退款率和单位作品成本的正式统计口径，再据此调整套餐升级、续费、优惠、邀请奖励参数和批量运营能力；当前累计成交与邀请注册面板不能替代分时间窗、分渠道、分用户群的完整经营分析。
- **账户与滥用治理**：明确年龄门槛、地区限制、可接受使用政策、批量注册/刷积分/盗用支付/自动化滥用指标，以及冻结、解冻、申诉和误伤复核流程。
- **供应商退出与可迁移性**：为模型、支付、SMTP、对象存储和通知服务保留停服、封号、涨价、配额耗尽和跨境政策变化时的替代与数据导出方案，确保账号、订单、媒体和用户数据不会被单一供应商锁死。

## 容易遗漏的上线控制项

以下事项经常不在“功能已完成”清单中，但会直接影响商业上线后的安全、成本和售后责任：

- **管理员高风险保护**：TOTP MFA、登录环境变化提醒、本人登录记录、13 项职责权限和后台成功/失败审计已提供技术能力；支付、用户、设置、密钥、存储和备份按用户要求取消逐次密码/MFA 复核。上线前仍须为生产管理员真实验证登录 MFA、身份验证器丢失恢复、Session 撤销、越权拒绝、异常登录处置和审计记录，并由负责人确定短会话、双人审批、紧急访问和定期权限复核规则。
- **密钥生命周期**：建立加密密钥、模型密钥、支付证书、维护令牌和 OSS 凭据的创建、保管、轮换、吊销和泄露应急流程；恢复备份时必须同时验证密钥可用性，不能只恢复数据库和媒体。
- **支付争议**：除退款外，明确拒付、支付争议、重复扣款、退款原路退回失败和支付商冻结账户的处理责任、证据留存及用户通知。
- **数据生命周期**：后台已提供显式维护入口，按管理员开关和批次清理具有明确技术到期时间的 Session、邮箱验证码、生成任务和无引用临时媒体；普通在线读写不再顺手执行无界清理，PostgreSQL 与文件 Provider 都使用稳定排序和有界批次。上线前仍须为对话、长期媒体、WebP 变体、审计、积分、订单、支付记录和 S3 对象确定适用地区的保留期限、用户删除触发、法律保留例外、调度负责人和删除结果核验，代码不会猜测这些制度。
- **存储与网络成本**：按本地磁盘、OSS 请求、存储容量、外网下行、CDN/代理和备份副本建立成本监控，设置容量、带宽和对象存储账单预警；S3 开关本身不等于成本治理。
- **故障沟通**：准备状态页或公告替代方案、维护通知模板、生成失败/退款延迟/上游停服的用户文案和客服升级路径，明确谁有权暂停某个模型或支付渠道。
- **无障碍与浏览器支持**：在目标浏览器和移动端完成键盘操作、焦点顺序、表单错误、对比度、动态状态播报、媒体替代文本和下载行为验收，并公布最低支持范围。
- **通知可达性**：验证注册、验证码、密码重置、支付结果和故障通知的实际送达；配置发信域名 SPF/DKIM/DMARC，监控退信与投诉，保留模板版本，并为通知供应商故障准备人工或站内公告兜底。
- **环境与回滚**：开发、预发布和生产必须隔离数据库、对象存储桶、支付商户、模型密钥、维护令牌和回调域名；每次发布都要有迁移前备份、冒烟检查、可替换镜像和回滚后数据兼容记录。
- **成本熔断**：后台已支持按实际积分计价配置单任务、单用户每日和全站每日生成成本上限，默认 `0` 表示不限制；PostgreSQL 使用事务级全站预算锁，文件 Provider 使用既有串行事务，退款释放当日占用。上线前仍须按真实模型价格与毛利设置生产值，并补齐供应商余额、媒体处理、存储与外网下行告警；积分保护不能代替上游配额、盗号识别和异常渠道暂停。
- **商业证据**：每项 P0/P1 控制项都应登记负责人、截止时间、环境、证据链接、复核人和复核日期；没有真实支付、通知、恢复、压测或安全演练证据时，仍标记为未完成。

这些控制项应落到负责人、截止时间、证据链接和复核结果；没有证据时继续标记为未完成，不要因为已有接口、后台开关或自动化测试就视为商业闭环。

## 商业上线证据矩阵

下面是上线前必须形成的最小证据。负责人可以由同一人兼任，但财务、法律、高风险运营操作和安全事件不应只由开发人员单独确认。

| 领域           | 最小完成证据                                                                                       | 上线闸门 |
| -------------- | -------------------------------------------------------------------------------------------------- | -------- |
| 支付与退款     | 每个启用渠道的下单、成功/失败、重复和迟到回调、整单退款、退款失败恢复、对账记录                    | 阻断     |
| 法律与授权     | 已审核的隐私政策、服务条款、退款和内容责任文本；注册协议版本与同意记录方案                         | 阻断     |
| 内容安全       | 违规分类、举报入口、人工复审、申诉、下架和审计记录各至少一条演练记录                               | 阻断     |
| 监控与值班     | 就绪探针、生成失败、支付失败和磁盘告警触发记录；值班人和升级路径                                   | 阻断     |
| 备份与恢复     | PostgreSQL、媒体、OSS、支付配置和加密密钥的备份清单；新环境恢复报告及 RPO/RTO                      | 阻断     |
| 容量与代理     | 分级并发与媒体热点压测报告，包含 P95/P99、连接池、锁等待、文件句柄、代理 IP、S3 下行和 429 误伤    | 阻断     |
| 生成任务与计费 | 真实媒体任务的超时、取消、重试、断线恢复、重复提交、失败退款和账本核对记录                         | 阻断     |
| 财务与定价     | 积分成本、模型成本、存储/带宽、手续费、税费和毛利基线；退款与拒付责任人                            | 阻断     |
| 权限与安全     | 管理员账号与职责清单、MFA 设备启用与恢复演练、越权拒绝、异常登录处置、人工审批规则和密钥轮换记录   | 阻断     |
| 客服与通知     | 客服入口、服务时间、SLA、投诉/隐私请求渠道；邮件域名和退信处理验证                                 | 灰度前   |
| 供应商退出     | 模型、支付、SMTP、OSS 的合同/限额/SLA、停服替代和数据导出验证                                      | 灰度前   |
| 数据生命周期   | 技术到期批次运行记录；对话、长期媒体、WebP、审计、积分、订单、支付和 S3 的保留、删除与法律保留规则 | 灰度前   |

### 证据登记模板

上线前将每个阻断项和灰度项各登记一行，不能只在文档中写“已配置”：

| 领域             | 负责人 | 截止时间 | 环境      | 证据链接/文件                    | 复核人 | 复核日期 | 状态   |
| ---------------- | ------ | -------- | --------- | -------------------------------- | ------ | -------- | ------ |
| 示例：支付与退款 | 待指定 | 待指定   | 沙箱/生产 | 待补充下单、回调、退款、对账记录 | 待指定 | 待指定   | 未完成 |

### 上线判定

1. 任一阻断项没有负责人、复核人和真实证据时，只能进入内部或封闭测试，不能宣称正式商业上线。
2. 代码测试、健康探针、后台开关和配置页面只能证明技术基础，不能替代支付商、OSS、邮件、代理、恢复和人工流程验收。
3. 灰度期间每周复核生成成功率、退款率、单位作品成本、存储增长、投诉和供应商异常；发现资金、隐私或内容安全风险时立即暂停对应渠道并保留用户数据与财务记录。
