---
title: TODO
description: 当前项目后续值得处理的事项
---

# TODO

本文档用来记录当前项目后续比较值得处理的事项。

- GitHub 发布部署验收：七套 Compose 与 Render Blueprint 的结构化部署契约已进入发布门禁，Render 已声明付费 Web/Worker、一次性安装令牌、Web 独立外部维护令牌、Web/Worker 共享的内部 Worker 令牌、私网调用、PostgreSQL 和 10GB 持久盘；两枚运行令牌各至少 32 字符且必须不同，生成 Worker 不再取得外部维护令牌，也不读取包含数据库、支付、模型、存储和安装令牌的完整 `.env`。本地发布检查已验证自定义 `distDir` 的 standalone 服务器、非空静态目录和完整公开品牌资源，生产 Dockerfile 也会在 amd64/arm64 镜像构建时加载 Sharp 并阻止缺少 libvips 的镜像发布。下一步仍需在干净 Linux 服务器执行 `docker compose config`、镜像拉取、PostgreSQL 健康检查、安装令牌错误/正确初始化、首管理员并发创建、安装后移除令牌、生成运维摘要与资源统计、Worker 心跳、图片 WebP 预览、逐项静态资源请求、HTTPS 反向代理和数据卷重启恢复，并在真实 Render 账号核对 Blueprint 资源、费用、首次部署、Worker 心跳与持久盘恢复。
- 短剧生产线后续：剧本工作台已完成最终四栏生产布局与独立 `.next-drama-final` 构建；下一步在生产等价登录态完成 1672×941/1440px/390px/430px 至少三轮截图对比，使用真实文本、图片、视频与音频渠道验收两阶段剧本分析、三种视频模式、镜头转码、配音混合、中文字幕烧录、剪映 5.x/6+ 草稿导入、任务取消和成片下载，并继续补充整集成片历史、长期成本归档和媒体访问压力测试。
- 上游可用性验收：本地自建 TCP 接口现由完整协议注册表和 GlobalAiOpc 预置表自动展开，已覆盖所有受支持协议的文本、图片、视频、音频、异步查询、二进制媒体、模型目录与鉴权响应，包括 `compatible`、`auto` 和仍可恢复的旧渠道配置；生产图片/视频适配器又经 `/api/ai/system/{channelId}` 内部代理完成 8 个图片和 12 个视频协议场景的文生/参考图、异步查询、逻辑模型白名单、计费头、协议鉴权和媒体读取集成测试，四文件协议专项总计 81 项通过。当前 PostgreSQL 审计实例的昱梦渠道已启用视频逻辑模型，官方 API 主机、完整 V2 创建/查询路径和已保存 Bearer Key 已通过不创建任务的只读验证；New API 渠道继续提供文本、图片和音频默认模型。不能把夹具、代理集成或只读查询当作真实供应商生成可用性。下一步分别保存昱梦 `videos_stable` 最小创建、参考视频、首尾帧、真实媒体、轮询、重试、失败退款和余额流水证据，并复核现有文本、图片和音频默认模型的生产质量、额度与 SLA。
- 创作 Agent 后续：为视频产物增加可信封面/关键帧抽取与多帧复盘，并在现有真实图片检查基础上补充主体、品牌元素和参考素材的视觉相似度判断；继续补齐长期积分归档、镜头版本差异对比、整集历史和成本质量分析。
- 统一生成执行器后续：页面 GET/SSE 已能唤醒同一持久任务并追回上游结果，普通 Agent 复盘也已转为持久 Worker 任务，PostgreSQL Run 事件具备 `LISTEN/NOTIFY` 即时唤醒与轮询兜底；下一步在默认 Docker、宝塔、外部数据库和至少两个应用实例中配置维护令牌、常驻并监控 `generation-worker`，验证用户完全关闭页面后仍会完成查询、媒体保存和复盘，并记录通知丢失、实例重启和租约接管证据。继续验收上游真实取消、重复创建防护、媒体引用统计与删除竞态。
- 参考素材部署验收：正式环境使用外部可访问的 `NEXT_PUBLIC_SITE_URL`，以无痕窗口验证 `/api/generation-log-assets/**` 匿名读取，并验证真实上游读取站内图片、视频和音频、生成一致性、历史恢复与临时文件清理；开发机回环地址不得静默退化为无参考生成。
- 第三方中转后续：基于现有逻辑模型绑定继续增加渠道熔断、权重路由、Webhook 回调和上游鉴权媒体转存，并用实际接入平台逐项维护协议验收样例。
- 协议平台后续：把当前渠道级自定义协议草稿关系化为不可变 AdapterRevision，补齐 OpenAPI/Postman/HAR 结构化解析、字段来源与版本差异、静态校验、发布、灰度和回滚；向昱梦上游取得并验证正式 V2 模型目录路径、鉴权和响应结构后再启用自动拉取，未确认前使用官方文档预置模型或手动维护模型 ID；复杂云签名、WebSocket、ComfyUI 工作流和专有上传会话通过受审核原生 Driver 接入，不允许后台执行脚本。
- 安全与并发后续：继续在多实例 PostgreSQL、真实反向代理和支付商回调压力下验收认证写入、退款 claim、事件重放和失败退款；媒体侧使用真实本地大视频与私有 S3 验证用户/签名/资源/IP 分层限流、GET/HEAD、32 MiB Range 分段、拖动播放、并发取消、文件句柄、连接池、出站流量和热点资源 429 误伤。
- 全站性能后续：31 个页面的首屏数据、请求数、分页/缓存、SEO 与已知风险矩阵已经建立，`/assets` 已改为服务端分页，Canvas 已拆分列表摘要与单项目详情；本地 PostgreSQL 已记录财务、生成概览、创作首页、Canvas、素材、短剧成本和会话分页的 `EXPLAIN (ANALYZE, BUFFERS)` 与可用索引路径，下一步仍需按生产等价数据量补充逐页响应字节、P50/P95/P99、连接池/锁等待、慢查询和正式构建/bundle 证据。
- 异步生成商业验收：使用真实图片、视频、音频和 Agent 渠道验证排队、超时、取消、显式重试、断线恢复、重复提交、卡死任务接管、失败退款和余额/流水/媒体登记核对；重试、人工补账、上游暂停和降级规则必须来自供应商公开约束、管理员配置或已有可验证契约，禁止再用拍脑袋的固定次数掩盖状态缺陷。
- 工程质量后续：ESLint、仓库级 Playwright E2E、Dependabot、CODEOWNERS、Issue/PR 模板、第三方许可证清单、镜像 SBOM 与签名基线已经接入；个人中心双分页、支付订单 SSE、图片/视频失败重试和 Agent 对话切换已纳入桌面、390px、430px 回归。下一步继续扩展其余登录后后台、支付、图片/视频、Canvas 和短剧的浏览器断言，逐步提高 Route Handler 契约测试覆盖率，并按真实职责拆分历史大文件，避免一次性重构全部路由。
- 商业上线验证：本地兼容签名回调已覆盖成功、重复、并发认领、无效签名和失败释放；仍需逐个支付商完成沙箱/生产前的下单、回调、重复/乱序/延迟回调、支付超时、全额退款和对账验收。支付宝官方支付与当面付必须分别验证，其中当面付补齐预下单二维码、扫码、二维码过期和异步通知证据。同时使用真实 PostgreSQL 压测并发领券、同券并发下单、取消/超时释放、迟到支付冲突和退款后不返券，核对支付商实收始终等于订单快照。当前自动退款按整笔订单处理，如商业规则需要部分退款，需单独补齐订单、支付流水、积分回收和会计口径。公开注册已保存条款/隐私版本、链接与同意时间；仍需由法律顾问按实际运营主体、服务地区、数据位置和退款规则复核服务条款、隐私政策、退款政策、知识产权与内容责任条款，并确定协议更新后的重新同意流程，现有页面只能作为产品基础文本。
- 邀请增长生产验收：使用真实 PostgreSQL、至少三个账号和真实/沙箱支付验证邀请访问、注册原子绑定、首单门槛、冷静期维护调度、重复支付回调、并发结算、月度/活动上限、同网络/支付身份风险信号、退款积分反向流水及已锁定/核销优惠券人工复核；上线前确定风控阈值、冻结通知、申诉、误伤恢复和运营值班制度。
- 生产可观测与容量：按部署环境选择可选的错误追踪、结构化日志、主机/容器指标和告警方案，补充数据库连接池使用率、慢查询、API P95/P99、支付成功率和生成成功率监控；完成 100/500/1000 并发分级压测后再决定是否引入 Redis、独立队列或扩大连接池，禁止未经压测机械增加基础设施。
- 商业运营后续：继续做服务器媒体备份与容量告警、整库/媒体/OSS 恢复演练、支付商账单真实样例库、账单自动抓取/周期触发对账、发票税务口径、客服与投诉升级、退款审核和异常支付基础风控。
- 商业安全与服务治理：异常登录环境提醒、本人分页登录记录、13 项管理员职责与 8 个职责预设，以及明确技术到期时间的 Session、验证码、生成任务和无引用临时媒体有界维护已完成代码；后台业务操作按用户要求取消逐次密码/MFA 复核。下一步仍需在生产账号/真实身份验证器和多管理员职责组合下演练登录、设备丢失恢复、越权拒绝、职责交接和审计核对，并建立模型密钥、支付证书、维护令牌与 OSS 凭据的轮换/吊销/泄露响应。拒付与支付争议、长期业务数据和 S3 对象的法定保留/删除制度、存储带宽成本预警、状态页/故障通知和无障碍验收证据仍未完成。
- 安全事件与供应商退出：单独形成数据泄露、密钥泄露、越权访问、私有媒体暴露和支付数据异常的分级响应手册，演练隔离、取证、通知、会话撤销、密钥轮换、备份恢复和复盘；同时为模型、支付、SMTP、OSS 和通知供应商保留停服/封号/涨价/跨境变化下的迁移、导出与替代方案。
- 商业上线控制项：验证注册、验证码、密码重置、支付结果和故障通知的实际送达，完成 SPF/DKIM/DMARC、退信投诉处理和通知兜底；隔离开发/预发布/生产的数据库、对象存储、支付商户、模型密钥和回调域名，并演练迁移前备份、冒烟、回滚及回滚后数据兼容。单任务、单用户每日和全站每日积分成本保护已由后台配置且默认不限制；下一步按真实模型成本和毛利确定生产额度，并补齐供应商余额、媒体处理、存储、带宽成本告警与异常渠道暂停规则。
- 用户权利与内容合规：在已实现注销申请、作品举报/申诉、人工处置、审计记录、用户范围分页导出和管理员删除前媒体快照的基础上，明确正式注销执行、创作数据删除、订单/支付/邀请/治理记录法定保留、媒体引用清理、身份复核与响应时限；使用隔离 PostgreSQL、本地媒体和真实 S3 分别演练无商业记录账号、存在受保护商业记录账号、对象删除失败与共享引用阻止，确定失败后的可恢复处置，不能把数据库级联成功等同于合规注销完成。建立侵权投诉、人工值班、处置时限、误伤复核和真实演练制度。短剧的用户内容确认 Gate 不能替代平台合规审核。
- 作品社区运营验证：在公开界面已实现点赞/取消、作者关注、公开创作者主页、本人关注/粉丝/点赞 Modal、取关/取消点赞/拉黑、互动通知和 7/30 天榜单的前提下，使用至少两个真实账号与公开作品完成关注/取消、双方拉黑与取消拉黑、列表即时刷新、粉丝关系、主页弹窗、点赞历史、用户/管理员下架、下架后永久删除、源素材保留、封禁联动和榜单稳定性验收；补做 390px/430px 实机布局验证，上线前仍必须补齐反刷监控、内容处置、隐私、审核和值班规则。
- 供应商与安全治理：建立模型、支付、SMTP、对象存储和通知服务的合同/配额/SLA/数据处理清单，明确停服替代、用户内容权属、AI 生成标识、年龄限制和可接受使用政策；依赖漏洞审计已纳入发布门禁，仍需完成 AGPL/第三方许可证、容器镜像与密钥泄露扫描及漏洞披露流程。
- 增长与体验后续：在经营看板已有累计订单成交、活动/用券成交和邀请注册基础上，补齐正式时间窗、收入、用户增长、留存、ARPU、复购、退款率与单位作品成本口径，建设首次使用引导、首个成功作品路径、套餐升级/续费策略和必要的批量运营能力；可靠进度信息优先展示任务阶段，不伪造上游剩余时间。
